services:
  agent:
    build:
      context: .
      target: runtime
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # Mount source for hot-reload during local development
      - ./agent:/app/agent:ro
